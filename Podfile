# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'BMI' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for BMI

  target 'BMITests' do
    inherit! :search_paths
# Twitter
  pod 'Fabric'
  pod 'TwitterKit'
  pod 'TwitterCore'
  pod 'Crashlytics'
    # Pods for testing
  end

  target 'BMIUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
